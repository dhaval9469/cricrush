import 'package:flutter/material.dart';

class BowlingMR extends StatelessWidget {
  const BowlingMR({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
